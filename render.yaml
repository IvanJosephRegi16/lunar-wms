services:
  - type: web
    name: lunars-erp-platform
    env: node
    buildCommand: npm install && npm run build
    startCommand: node scripts/init-db.js && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        sync: false
      - key: DATABASE_URL
        sync: false
